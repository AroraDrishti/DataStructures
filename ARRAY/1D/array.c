#include <stdio.h>
#define MAX 10

int main()
{
    // int arr[MAX]; 
    // int i, n;

    // printf("Enter size of the array\n");
    // scanf("%d", &n);

    // //taking input of the elements
    // for(i=0;i<n;i++)
    // {
    //     printf("Enter element %d of the array\n ", i);
    //     scanf("%d \n", &arr[i]);
    // }

    // //displaying elements
    // for(i=0;i<n;i++)
    // {
    //     printf("%d ", arr[i]);
    // }

    int x = 210;
    if(x=19)
    {
        printf(x);
    }
    

    return 0;
}

    
    return 0;   
}